make_features <- function(x, xnames, .funs){
  ### INCOMPLETE

  # x <- unlist(strsplit(x, split = ""))
  # for (i in seq_along(.funs)){
  #   aad <- sapply(x, Peptides::aaDescriptors)
  #   aad.sum <- apply(X = aad, FUN = .funs[i], MARGIN = 1)
  # }
  #
  # indfeat <- as.data.frame(t(rowMeans(sapply(x, aaDescriptors))))
  # names(indfeat) <- xnames
  # return(indfeat)
}
